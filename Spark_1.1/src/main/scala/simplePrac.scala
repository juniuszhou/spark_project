object simplePrac{
  def sub(i: Int, j:Int) = i - j
  
  class abc{
    Product =>
      
      def a = {
        self: Product => 
      }
      
  }
  
  def main(args : Array[String]){
    val str: String = "set,id,name=abc,123,456"
      str.split("=", 2).map(_.trim) 
  }
}

